services:
  
type: worker
  name: bot-telegram
  runtime: python
  plan: free
  buildCommand: "pip install -r requirements.txt"
  startCommand: "python main.py"
  envVars:
key: TOKEN
    sync: false